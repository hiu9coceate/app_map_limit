/// Định nghĩa các hằng số cho menu
class MenuConstants {
  MenuConstants._();

  // Animation durations
  static const animationDuration = Duration(milliseconds: 300);

  // Menu width
  static const menuWidth = 280.0;

  // Menu item height
  static const menuItemHeight = 60.0;
}
